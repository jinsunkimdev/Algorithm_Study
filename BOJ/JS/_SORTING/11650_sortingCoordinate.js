let input = []
require('readline').createInterface(process.stdin, process.stdout)
.on('line', (line) => {
	input.push(line.trim())
}).on('close', () => {
	const N = input.shift()
	let axisX, axisY = []
	for(number of input)
	{
		[axisX, axisY].push(number.split("")) 
	}
	console.log(axisX,axisY)
})